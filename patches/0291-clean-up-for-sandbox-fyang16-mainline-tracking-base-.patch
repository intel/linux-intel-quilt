From 90c50433ab2c88ea9213478840d2373d67d437dd Mon Sep 17 00:00:00 2001
From: "Randy G. Duran" <randy.g.duran@intel.com>
Date: Tue, 2 Apr 2019 13:12:16 -0700
Subject: [PATCH 291/292] clean up for
 sandbox/fyang16/mainline-tracking-base-5.0-ww10.3 part 3

---
 include/linux/tee_drv.h | 12 ++++++------
 1 file changed, 6 insertions(+), 6 deletions(-)

diff --git a/include/linux/tee_drv.h b/include/linux/tee_drv.h
index e0e80a8ed4e4..723202bd9174 100644
--- a/include/linux/tee_drv.h
+++ b/include/linux/tee_drv.h
@@ -584,7 +584,7 @@ void tee_client_close_context(struct tee_context *ctx);
  * @vers:	Pointer to version data
  */
 void tee_client_get_version(struct tee_context *ctx,
-			   struct tee_ioctl_version_data *vers);
+			    struct tee_ioctl_version_data *vers);
 
 /**
  * tee_client_open_session() - Open a session to a Trusted Application
@@ -597,8 +597,8 @@ void tee_client_get_version(struct tee_context *ctx,
  * is TEEC_SUCCESS the session identifier is available in @arg->session.
  */
 int tee_client_open_session(struct tee_context *ctx,
-			   struct tee_ioctl_open_session_arg *arg,
-			   struct tee_param *param);
+			    struct tee_ioctl_open_session_arg *arg,
+			    struct tee_param *param);
 
 /**
  * tee_client_close_session() - Close a session to a Trusted Application
@@ -608,7 +608,7 @@ int tee_client_open_session(struct tee_context *ctx,
  * Return < 0 on error else 0, regardless the session will not be
  * valid after this function has returned.
  */
-+int tee_client_close_session(struct tee_context *ctx, u32 session);
+int tee_client_close_session(struct tee_context *ctx, u32 session);
 
 /**
  * tee_client_invoke_func() - Invoke a function in a Trusted Application
@@ -620,8 +620,8 @@ int tee_client_open_session(struct tee_context *ctx,
  * Returns < 0 on error else see @arg->ret for result.
  */
 int tee_client_invoke_func(struct tee_context *ctx,
-			  struct tee_ioctl_invoke_arg *arg,
-			  struct tee_param *param);
+			   struct tee_ioctl_invoke_arg *arg,
+			   struct tee_param *param);
 
 
 #endif /*__TEE_DRV_H*/
-- 
2.21.0

